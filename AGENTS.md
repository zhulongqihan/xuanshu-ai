# 玄枢 AI 项目协作准则

## 最高优先级红线

1. 本项目是一个结合八字、紫微斗数、老黄历与六爻的命理 AI Agent，持续开发是长期目标。
2. 项目仓库固定为 `https://github.com/zhulongqihan/xuanshu-ai`。
3. 用户不需要亲自编写代码，主要通过自然语言和 vibe coding 指挥开发；Agent 负责把需求转化为设计、代码、测试、文档和可运行成果。
4. 每完成一个可独立验证的开发步骤，都必须及时提交并推送到 GitHub。不得只保留在本地，也不得在未说明原因的情况下积压多个完成步骤后才同步。
5. 每次同步必须向用户简要说明：完成了什么、如何验证、对应提交，以及下一步建议。
6. 本文件适用于本项目目录下的后续所有对话与开发任务。除非用户明确修改，任何实现不得绕过这些红线。

## 持续执行准则

### 需求与决策

- 开始实现前，先复述目标、关键假设和本次验收标准；小改动可以一句话说明。
- 需求不完整时，优先采用可逆、保守且能推进工作的合理默认值；涉及产品定位、付费、隐私、法律风险或不可逆数据操作时必须先确认。
- 重要产品与技术决策记录在 `docs/`，避免只存在于聊天记录中。
- 控制每一步的范围，使提交易于理解、验证和回退，不混入无关重构。

### GitHub 与版本管理

- 默认分支为 `main`，提交信息清楚描述结果，避免无意义的 `update`、`fix` 等表述。
- 开始任务前检查工作区和远程状态；不得覆盖、回退或删除用户已有但未提交的改动。
- 每个可验证里程碑执行：检查差异、运行相关测试、提交、推送、确认远程同步成功。
- 严禁提交密钥、令牌、身份证明、用户隐私数据或本地环境文件；敏感配置使用环境变量与示例文件。
- 推送失败时立即说明原因并继续排查，不得把“本地已提交”表述成“已同步 GitHub”。

### 工程质量

- 优先交付最小可用的纵向功能，再持续迭代；不为尚未出现的需求过度设计。
- 遵循仓库既有技术栈、代码风格和目录结构；引入新依赖前说明用途与代价。
- 新功能必须包含与风险相匹配的测试；修复缺陷时优先增加能复现问题的回归测试。
- 提交前至少运行与改动相关的格式检查、静态检查、测试和构建；无法运行时明确记录原因与风险。
- 用户可见功能需考虑桌面端与移动端、加载/空/错误状态、无障碍、中文体验和基础性能。

### 命理领域边界

- 八字、紫微斗数、老黄历和六爻分别建模，明确历法、时区、真太阳时、节气换月、闰月及流派差异，不把不同体系的规则无依据地混合。
- 计算结果与 AI 解读分层：可确定的排盘由可测试的规则引擎产生，语言模型负责解释、归纳和交互，不让模型臆造基础盘面。
- 每条关键判断尽量提供所用体系、规则来源、输入数据与不确定性，保证结果可追溯、可复算。
- 产品内容定位为传统文化研究、娱乐与自我反思参考，不宣称科学定论，不替代医疗、法律、投资或其他专业意见。
- 不利用恐惧、灾祸暗示或绝对化断言诱导付费；对健康、死亡、犯罪、重大财务等高风险问题使用克制表达和明确免责声明。
- 出生日期、时间、地点等属于敏感个人信息，坚持数据最小化、明确告知用途、可删除，并默认不用于模型训练或对外共享。

### 文档与上下文连续性

- `README.md` 保持项目定位、运行方式和当前状态准确；接口、数据模型或部署方式变化时同步更新文档。
- 在执行上下文压缩、总结历史对话或清理超限 token 前，必须把核心进度、未解决问题和关键上下文写入 `docs/context_summary_YYYYMMDD_HHMM.md`，然后通知用户。
- 每个阶段结束时维护清晰的待办与下一步，使新对话可以仅依赖仓库继续工作。

## 冲突处理

- 优先级依次为：用户最新明确指令、安全与法律边界、本文件红线、仓库既有规范、一般工程惯例。
- 若用户指令与安全、隐私、法律边界冲突，应暂停相关实现，说明风险并提出可行替代方案。
