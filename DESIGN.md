<!-- SEED: re-run $impeccable document once there's code to capture the actual tokens and components. -->
---
name: 玄枢 AI
description: 安静、精确、证据优先的个人命理研究工作台
---

# Design System: 玄枢 AI

## Overview

**Creative North Star: “清晨案头的星历工作台”**

界面服务于需要集中注意力的个人研究：像一张整理清楚的现代工作台，时间、盘面、证据和
咨询各有位置，用户可以迅速扫读，也能停下来逐层核对。它借鉴 Apple Calendar 的时间层级、
Linear 的克制密度和 Obsidian 的本地研究感，但不复制任何一种产品的视觉外壳。

产品拒绝黑金玄学网站、紫色渐变 AI 工具、米黄仿古纸张和卡片式 SaaS 首页。视觉表达
必须现代、中文友好、功能优先；传统文化感来自数据结构、术语和内容，而不是装饰符号。

**Key Characteristics:**

- 清晰的时间与证据层级
- 高密度但稳定的研究布局
- 本地、私密、可信的状态表达
- 稀少而有意义的颜色和动效

## Colors

采用 Restrained 策略：纯中性背景、带轻微天空蓝倾向的主色，以及独立的警告和成功语义色。
主色只用于当前导航、主要操作、焦点和可信状态，不承担装饰任务。

**The Clear Sky Rule.** 主色锚定柔和钴蓝/靛蓝，具体 OKLCH 色阶在实现时以对比度测试确定。

**The Neutral Desk Rule.** 页面背景保持无彩或极低彩度，不使用米黄、羊皮纸和仿古暖色作为底色。

**The Evidence Color Rule.** 四术不得各自占据大面积主题色；体系差异优先由标签、标题和结构表达。

## Typography

使用单一的人文无衬线体系，优先调用本机中文系统字体，避免远程字体成为本地应用的启动依赖。

**Character:** 字形清楚、数字稳定、长时间阅读不疲劳。术语与普通解释使用同一字体家族，
通过字号、字重和对齐建立层级，不以书法字体制造传统感。

### Hierarchy

- **Display:** 仅用于页面主标题，固定字号，不随视口连续缩放。
- **Headline:** 用于工作区和主要面板标题，紧凑但保留清晰行高。
- **Title:** 用于分组、盘面宫位和证据条目。
- **Body:** 正文最小 16px，解释性段落控制在 65-75 个字符宽度。
- **Label:** 最小 12px，不使用负字距或大面积全大写英文。

**The Readable Chinese Rule.** 任何紧凑度优化都不能以缩小中文正文或降低对比度为代价。

## Elevation

默认采用平面和色调分层。页面、侧栏、工具栏和内容区通过背景明度与细分隔线区分；阴影只用于
弹出菜单、对话框和需要脱离文档流的临时层，不为静态卡片添加宽而软的装饰阴影。

**The Flat-by-Default Rule.** 静态内容平铺在工作区；只有真实浮层才能获得投影。

## Components

首版组件遵循熟悉的产品控件语汇：图标按钮用于工具，分段控件用于模式，开关处理二元设置，
菜单处理选项集合，标签页切换盘面视图。所有交互组件必须覆盖默认、悬停、焦点、按下、禁用、
加载和错误状态。

- 按钮和输入使用轻微曲率，卡片和独立工具容器最大 8px 圆角。
- 主要操作使用实色主色按钮，次要操作使用文字或低强调边框。
- 证据条目使用可展开列表，不嵌套成多层卡片。
- 导航在桌面使用稳定侧栏，在移动端变为底部主导航或抽屉，不压缩成不可读窄栏。
- 加载使用保持布局尺寸的骨架；空状态直接提供下一项可执行操作。

## Do's and Don'ts

### Do:

- **Do** 让首页直接呈现今日、档案和继续咨询等真实任务。
- **Do** 保持盘面、证据和解释之间可视化对应关系。
- **Do** 使用统一图标、控件尺寸、焦点环和语义状态。
- **Do** 在桌面与移动视口检查最长中文词组、错误信息和加载状态。

### Don't:

- **Don't** 使用“黑底、金字、龙纹、八卦纹和高饱和红色堆叠的传统算命网站”风格。
- **Don't** 使用“紫色渐变、发光球体、玻璃卡片和聊天气泡占据一切的通用 AI 工具”风格。
- **Don't** 使用“米黄羊皮纸、书法字体和伪古籍纹理营造的廉价东方感”。
- **Don't** 制作“由相同图标卡片组成、用巨大指标冒充价值的 SaaS 仪表盘”。
- **Don't** 用颜色、动画或装饰强化灾祸、死亡、破财等恐惧内容。
- **Don't** 嵌套卡片、使用渐变文字、侧边彩条、超大圆角或宽模糊阴影。
