# 项目上下文摘要（2026-08-22 03:20）

## 当前项目

- 项目：玄枢 AI，仓库 `F:\xuanshu-ai`。
- 远程仓库：`https://github.com/zhulongqihan/xuanshu-ai.git`。
- 默认分支：`main`。
- 当前最新远程提交：`2b4d319f014ba7ae354d8d1ba3efcfc86d8fddf7`。
- 工作区在本次继续执行前为干净状态，项目状态审计为 `ok`。

## 已完成事实

- 四个规则引擎主链路已实现并通过现有自动化检查：八字、紫微斗数、老黄历、六爻。
- 档案、排盘修订、证据展示、咨询降级、备份/恢复/删除、桌面与移动端基础体验已完成。
- 固定单元格证据、来源登记、200 条 Agent 基线评测协议、真实模型评测启动器、独立参考案例模板、发布检查脚本均已落库。
- 最近一次完整 `pnpm check` 已通过：Agent 23 通过 1 跳过，Domain 81 通过 1 跳过，Web 38 通过，生产构建通过。
- 现有正式发布检查在本机能正确识别真实模型密钥、参考案例目录和第二台 Windows 人工验收尚未提供。

## 最新用户决策（优先于旧路线图）

用户明确要求跳过以下三项外部门禁并继续完成仓库内软件：

1. 真实模型 200 条评测；
2. 100 条紫微与 100 条六爻独立参考案例；
3. 第二台 Windows 的人工验收。

这三项被定义为“本轮延期的正式发布证据”，不是“已经通过”。项目可以在透明标记下完成首版候选发布范围，但不得据此宣称正式发布、黄金集达标或跨设备已验收。

## 当前未解决事实及影响

- `OPENAI_API_KEY` 尚未配置，真实模型评测仍为未执行；影响正式发布证据，不阻止候选发布检查。
- 独立参考案例目录仍为 0/0；影响规则引擎外部黄金集证据，不阻止候选发布检查。
- 第二台 Windows 尚未验收；影响跨设备发布声明，不阻止候选发布检查。

## 本轮下一步

1. 先更新权威路线图，记录“外部门禁延期但不视为通过”。
2. 更新 README、Windows 清单、模型评测、参考案例等派生文档，保持当前范围一致。
3. 给发布检查增加显式的候选发布模式：继续执行工程质量检查，跳过三项外部证据检查，并在输出中明确未通过、仅延期。
4. 运行候选发布检查、差异检查和项目状态审计。
5. 提交并推送本独立里程碑，向用户报告提交、验证结果和后续补证路径。

## 继承规则

本文件是上下文压缩的存档，不替代 `docs/quality-and-roadmap.md`。如果它与更新后的权威路线图冲突，以最新用户决策和权威路线图为准。
