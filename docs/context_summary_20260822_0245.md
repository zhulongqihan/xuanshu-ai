# 玄枢 AI 项目上下文摘要

生成时间：2026-08-22 02:45（Asia/Shanghai）

## 当前目标

执行既定项目计划，持续推进玄枢 AI 从工程闭环到可发布产品，直到主要功能、质量门禁、模型评测和 Windows 发布验收均有可复核证据。

## 权威状态

- 项目仓库：`https://github.com/zhulongqihan/xuanshu-ai.git`
- 默认分支：`main`
- 权威路线图：`docs/quality-and-roadmap.md`
- 最近提交：`f3a3ce5 fix: show active rule engines on dashboard`
- 最近提交已推送到 GitHub `main`。
- 上一次项目状态审计：`status=ok`，派生文档与权威路线图一致。

## 最近已完成

### 浏览器端到端验收

使用独立的临时本地数据目录完成生产服务浏览器验收，覆盖：

1. 创建用户档案并确认时区。
2. `/charts` 展示八字事实、强弱、起运和证据。
3. `/ziwei` 展示十二宫、星曜、大限和证据。
4. `/almanac` 展示离线历法、农历、干支、节气、建除和证据。
5. `/liuyao` 自动起卦并保存“鼎 → 泰”，展示 18 次原始爻掷结果、完整表格和证据。
6. `/consult` 在未配置模型密钥时给出明确、可操作的提示，不崩溃。
7. `/settings` 完成备份下载、JSON 解析、清空、上传恢复，恢复后档案可见。
8. 服务停止后确认 3000 端口释放。

### 首页状态修复

首页此前把已经可用的四类规则引擎显示成“待接入”，已改为展示：

- 八字：已接入 · 子平规则 1.0.0
- 紫微斗数：已接入 · 三合基础盘 1.0.0
- 老黄历：已接入 · 协纪辨方书 1.1.0
- 六爻：已接入 · 文王卦 1.0.0
- 历法状态：离线历法已就绪
- 模型状态：根据本机配置动态显示

对应提交：`f3a3ce5`。

## 验证结果

最近一次完整 `pnpm check` 已通过：

- Agent：23 passed，1 skipped（24）
- Domain：80 passed，1 skipped（81）
- Web：38 passed
- 生产构建成功，核心页面和 API 路由均生成。
- `pnpm audit --prod` 此前通过，无生产依赖漏洞。

Windows 特有运行注意：生产服务运行时直接执行 Next 构建会因 `.next/standalone` 被占用触发 `EBUSY`；先用受保护的停止脚本停止服务，再执行构建即可通过。正常启动脚本检测到健康服务后会复用服务，不会重复构建。

## 尚未完成

- 尚未配置 `XUANSHU_AI_API_KEY`，因此 200 条真实模型评测尚未执行。
- 紫微斗数独立人工参考案例仍为 0/100，六爻独立人工参考案例仍为 0/100；工程测试不能替代参考案例门禁。
- 尚未在另一台真实 Windows 机器或干净虚拟机完成安装、升级、启动、验收和清理。
- 需要继续确认首页修复是否应补充自动化回归测试。

## 下一步

1. 检查最新 Git 状态和远程同步状态。
2. 检查现有 Web 测试结构；若成本合理，补首页规则引擎状态回归测试。
3. 运行相关测试和完整质量检查，形成新的可验证里程碑并提交推送。
4. 在用户本机配置模型密钥后，执行真实模型 200 条评测并记录结果。
5. 补齐紫微斗数与六爻的独立人工参考案例，再决定是否放行发布。
6. 完成第二台 Windows 的发布验收。

## 约束与判断

- 不把工程自测、生成样例或源码候选当作人工黄金案例。
- 不把模型解读当作确定性排盘事实。
- 不要求用户在聊天中发送密钥；模型密钥只在用户本机配置。
- 当前不能宣称软件已经全部完成，发布证据链仍缺真实模型评测、独立参考案例和第二台 Windows 验收。
