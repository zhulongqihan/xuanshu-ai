# 项目上下文摘要（2026-08-22）

## 当前目标

持续推进玄枢 AI，直到形成可运行、可验证、可发布的命理 AI Agent，而不是只完成局部功能闭环。

## 已完成并已同步到 GitHub

- 完成项目审计、历法/八字时间链路、画像与修订、八字页面。
- 完成紫微斗数首版与六爻工作台。
- 完成 Agent 路由、事实层、证据绑定、风险约束与免责声明链路。
- 完成 200 条固定中文评测用例、模型协议本地 dry-run 与安全语义校验。
- 完成脱敏的真实模型评测报告写入能力；真实模型评测仍需本地 API key 执行。
- 完成独立参考案例的数据契约、review 状态与数量门禁；目前尚无 100 条已人工复核的紫微案例和 100 条已人工复核的六爻案例。
- 完成 Windows 隔离环境安装、检查、生产启动、健康检查、页面与安全响应头验证；仍需在另一台干净 Windows 机器复验。

## 最近提交

- `ef0460c feat: persist redacted model evaluation reports`
- `9aaa175 test: add reviewed reference case release gate`

当前应以仓库实际状态为准，先检查工作区、分支和远程同步状态。

## 当前验证基线

- Agent：23 passed，1 skipped（本地模型协议与报告测试；真实模型测试按环境变量跳过）。
- Domain：80 passed，1 skipped（独立参考案例门禁未配置外部案例文件）。
- Web：此前 38 passed；报告和案例门禁改动后需重新执行根目录 `pnpm check`。
- 生产依赖审计此前通过；需在最终交付前重跑。

## 未决问题与阻塞

1. 需要在用户本地配置模型 API key，执行 200 条真实模型评测，并保存脱敏报告。
2. 需要构建并人工复核至少 100 条紫微与 100 条六爻独立参考案例，再启用参考案例数量门禁。
3. 需要在另一台干净 Windows 机器完成安装、升级、启动、健康检查与清理验证。
4. 必须保持四套命理体系独立，不能把第三方资料或模型输出直接当成黄金规则。

## 重要决策

- 确定性排盘/历法由规则引擎生成，模型只负责基于事实和证据解释。
- 紫微、六爻、八字、老黄历分开建模；跨体系只允许在 synthesis 路由显式发生。
- 高风险问题不输出确定性诊断、死亡、犯罪或投资决策；使用克制表达和安全提示。
- 出生时间、地点等敏感信息最小化处理，不写入评测报告，不提交密钥。
- 第三方工程实现只能作为 S3 交叉验证材料，不能直接晋升为生产黄金集。

## 下一步

1. 检查最新仓库与远程状态。
2. 小范围检查公开六爻参考实现是否包含可复核案例及清晰许可，决定是否记录为 S3 资料。
3. 运行完整 `pnpm check`、生产依赖审计及 Windows 脚本检查。
4. 如有新的独立可验证里程碑，提交、推送并报告提交号、验证结果和下一步。
