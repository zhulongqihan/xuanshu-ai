# 玄枢 AI 上下文恢复摘要

更新时间：2026-07-18 00:57 +08:00

## 当前可信状态

- 仓库：`https://github.com/zhulongqihan/xuanshu-ai`
- 分支：`main`
- 当前已推送提交：`bf968789fb54fb4e407eb51f93ce0730e2432780`
- GitHub Actions：Windows 与 Ubuntu 双平台 CI 均通过。
- M0 与 M1 已完成，项目进入 M2。

## 本轮完成

1. `a375818`：增加 Windows 桌面启动器、本地健康接口、健康接口测试和双平台 CI。
2. `bf96878`：修复应用数据路径依赖宿主平台的缺陷，使 Windows 与 POSIX 路径计算可在任意 CI 宿主上确定复现。
3. Windows PowerShell 5.1 实测通过：脚本解析、快捷方式创建、生产启动、SQLite 初始化、健康接口和 `127.0.0.1` 回环监听。
4. Playwright 实测通过：桌面与移动视口遍历 8 个页面，无 HTTP 错误、标题缺失、横向溢出或控制台告警。

## 持久决策

- 确定性计算与 AI 解读继续分层；模型不可生成基础盘面事实。
- 应用数据默认保存在本机，并允许 `XUANSHU_AI_DATA_DIR` 同时覆盖数据库与日志根目录。
- 本地健康检查不主动请求模型端点；`model.configured` 只表示所引用的密钥环境变量是否存在。
- Windows PowerShell 脚本保留 UTF-8 BOM，以兼容 Windows PowerShell 5.1 的中文解析行为。

## M2 精确下一步

1. 先定义历法归一化输入与输出契约，覆盖公历/农历原始输入、IANA 时区、出生地、时间精度、真太阳时策略和候选盘。
2. 评估并选择历法、时区、节气与天文计算依赖，记录许可证、数据范围、误差和可离线性。
3. 建立人物档案的创建、读取与删除纵向流程，保留原始输入、归一化结果、规则版本和输入哈希。
4. 增加边界测试：23:00/00:00、节气前后五分钟、闰月、历史时区、夏令时和出生时间未知。

## 已知非阻断项

- `work/` 与 `output/playwright/` 中存在被 Git 忽略的本机验收产物，不进入提交。
- 当前业务页面仍是诚实的空状态；历法计算与人物档案交互属于 M2，尚未实现。
